query getTestSuite($id: ID!, $teamCount: Int) {
    suite: node(id: $id) {
        ... on Suite {
            id
            uuid
            # @genqlient(pointer: true)
            applicationName
            # @genqlient(pointer: true)
            color
            defaultBranch
            # @genqlient(pointer: true)
            emoji
            name
            slug
            teams(first: $teamCount, order: NAME) {
                edges {
                    node {
                        id
                        accessLevel
                        team {
                            id
                        }
                    }
                }
            }
        }
    }
}
