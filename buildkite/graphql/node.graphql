query getNode(
    $id: ID!
) {
    node(id: $id){
        ... on ClusterQueue {
            ...ClusterQueueValues
        }
        ... on ClusterToken {
            ...ClusterAgentTokenValues
        }
        ... on TeamMember {
            ...TeamMemberValues
        }
    }
}