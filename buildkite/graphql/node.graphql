query getNode(
    $id: ID!
) {
    node(id: $id){
        ... on ClusterToken {
            ...ClusterAgentTokenValues
        }
        ... on TeamMember {
            ...TeamMemberValues
        }
        ... on Team {
            ...TeamFields
        }
    }
}