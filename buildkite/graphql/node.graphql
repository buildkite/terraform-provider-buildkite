query getNode(
    $id: ID!
) {
    node(id: $id){
        ... on ClusterQueue {
            ...ClusterQueueValues
        }
        ... on TeamMember {
            ...TeamMemberValues
        }
    }
}